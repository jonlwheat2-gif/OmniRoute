- **feat(dashboard):** Feature-flag card descriptions are no longer silently lost to a hard two-line clamp. The Settings header gains an **"On hover / Full text"** control that switches between the compact two-line clamp — which now reveals the complete description in a hover tooltip — and the always-expanded description, with the choice persisted in `localStorage` ([#12740](https://github.com/diegosouzapw/OmniRoute/pull/12740)) — thanks @jonlwheat2-gif (with thanks to @retroamx for the #13189 card-render harness)
